package Dtype;

public class Study_ {
    public static void main(String[] args) {
        // new 키워드를 이용해서 새로운 생성자 만들기

        // equals
        // 일치하는지 확인하는 법

        // cotains
        // 포함되어있는지 확인하는 법 -> \ 넣어줘야함

        // split
        // "" 기준으로 쪼개기

        // String Formatting
        // 문자열에 변수 값을 삽입하거나 형식을 지정할 수 있음
        // MessageFormat
        // 문자열 포맷을 지정할 수도 있음




        // array
        // 내가 넣으려고 하는 자료형 [], 변수명 = new (자료형)[길이];
        // 예약! 자리 수 없이 예약 불가능 (배열, Java Array)
        // intArray 가능

        // 배열의 길이 .length (속성) String -> length() -> 메소드

        // ArrayList 만들기 (의존성 바로 불러오기 : Alt + Enter)

        // Wrapper class like Integer
        // int -> 기본 자료형 -> 클래스가 아니라서 메소드나 다른 기능 X
        // 자바에서의 클래스처럼 -> int가 아니라 int와 유사한 새로운 클래스 (Wrapping)
        // Wrapper Class. -> int -> Integer, double -> Double...

        // ArrayList에 원소 CRUD (Create - , Read - 불러오기, Update - 수정하기, Delete - )


    }
}